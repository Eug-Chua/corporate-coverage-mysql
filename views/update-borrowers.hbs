{{!-- {{#extends "base"}}

{{#block "content"}}
<h1>Update Borrower</h1>
<form method="POST">
    <div class="mb-3">
        <label>Borrower Name</label>
        <input type="text" name="borrower_name" class="form-control" value="{{borrower.borrower_name}}"/>
    </div>
    <div class="mb-3">
        <label>Obligor Risk Rating</label>
        <input type="text" name="risk_rating" class="form-control" value="{{borrower.risk_rating}}"/>
    </div>
    <div class="mb-3">
        <label>Product Name</label>
        <input type="text" name="product_name" class="form-control" value="{{product.product_name}}"/>
    </div>
    <div class="mb-3">
        <label>Loan Amount(USD mm)</label>
        <input type="text" name="loan_amount" class="form-control" value="{{sale.loan_amount}}"/>
    </div>
    <div class="mb-3">
        <label>Description</label>
        <input type="text" name="description" class="form-control" value="{{borrower.description}}"/>
    </div>
    <input type="submit" value="Update" class="btn btn-primary"/>
</form>
{{/block}}

{{/extends}} --}}

{{#extends "base"}}

{{#block "content"}}
<h1>Update Borrower</h1>
<form method="POST">
    <div class="mb-3">
        <label>Borrower Name</label>
        <input type="text" name="borrower_name" class="form-control" value="{{borrower.name}}"/>
    </div>
    <div class="mb-3">
        <label>Obligor Risk Rating</label>
        <input type="text" name="risk_rating" class="form-control" value="{{borrower.obligor_risk_rating}}"/>
    </div>

    {{!-- {{#each sales}}
    <div class="mb-3">
        <label>Product for Sale ID {{this.sale_id}}</label>
        <input type="hidden" name="sales[{{@index}}][sale_id]" value="{{this.sale_id}}"/>
        <select name="sales[{{@index}}][product_id]" class="form-control">
            {{#each this.products}}
            <option value="{{this.product_id}}" {{#if this.isSelected}}selected{{/if}}>{{this.name}}</option>
            {{/each}}
        </select>
        <label>Loan Amount (USD mm)</label>
        <input type="text" name="sales[{{@index}}][loan_amount]" class="form-control" value="{{../amount}}" placeholder="Loan Amount"/>
    </div>
    {{/each}} --}}
    <div class="mb-3">
        <label>Product for Sale ID {{#if sales.sale_id}}{{sales.sale_id}}{{else}}New{{/if}}</label>
        <input type="hidden" name="sale_id" value="{{#if sales.sale_id}}{{sales.sale_id}}{{/if}}"/>
        <select name="product_id" class="form-control">
            <option value="">Select a Product</option>
            {{#each products}}
            <option value="{{this.product_id}}" {{#eq this.product_id ../sales.selected_product_id}}selected{{/eq}}>{{this.name}}</option>
            {{/each}}
        </select>
        <label>Loan Amount (USD mm)</label>
        <input type="text" name="loan_amount" class="form-control" value="{{sales.amount}}" placeholder="Enter Loan Amount"/>
    </div>
    {{!-- {{#each sales}}
    <div class="mb-3">
        <label>Product for Sale ID {{#if this.sale_id}}{{this.sale_id}}{{else}}New{{/if}}</label>
        <input type="hidden" name="sales[{{@index}}][sale_id]" value="{{#if this.sale_id}}{{this.sale_id}}{{else}}new{{/if}}"/>
        <select name="sales[{{@index}}][product_id]" class="form-control">
            <option value="">Select a Product</option>
            {{#each ../products}}
            <option value="{{this.product_id}}" {{#if this.isSelected}}selected{{/if}}>{{this.name}}</option>
            {{/each}}
        </select>
        <label>Loan Amount (USD mm)</label>
        <input type="text" name="sales[{{@index}}][loan_amount]" class="form-control" value="{{this.amount}}" placeholder="Enter Loan Amount"/>
    </div>
    {{else}}
    <div class="mb-3">
        <p>No sales information available. Please add new sales data.</p>
    </div>
    {{/each}} --}}


    <input type="submit" value="Update" class="btn btn-primary"/>
</form>
{{/block}}

{{/extends}}
